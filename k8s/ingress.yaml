apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-python-app
spec:
  rules:
  - host: "sample-python-app.test.com"
    http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: sample-python-app
              port:
                number: 8080
